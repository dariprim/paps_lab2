namespace Lab2
{
    // ================ DIRECTOR (Распорядитель) ================
    // Согласно теории: "Director - распорядитель: конструирует объект, 
    // пользуясь интерфейсом Builder"
    public class TripDirector
    {
        // Метод, который конструирует объект по шагам
        // Согласно теории: "распорядитель уведомляет строителя о том, 
        // что нужно построить очередную часть продукта"
        public Trip Construct(TripBuilder builder)
        {
            Console.WriteLine($"\n🚀 НАЧАЛО КОНСТРУИРОВАНИЯ ПОЕЗДКИ...");
            
            // Шаг 1: Создаем транспорт
            builder.BuildVehicle();
            
            // Шаг 2: Назначаем водителя
            builder.BuildDriver();
            
            // Шаг 3: Добавляем пассажиров
            builder.BuildPassengers();
            
            Console.WriteLine($"✅ КОНСТРУИРОВАНИЕ ЗАВЕРШЕНО\n");
            
            // Возвращаем готовый продукт
            return builder.GetTrip();
        }
    }
}